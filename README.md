### How to run:
`./build.sh`

`wasm-pack build --target web`

`wasm-pack build --target web`
`dir pkg`
`copy pkg\*.js .`
`copy pkg\*.wasm .`

`wasm-pack --version`
`cargo install wasm-pack`

`npm install -g http-server`

`http-server`
